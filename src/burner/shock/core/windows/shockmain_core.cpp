
// See License.md for license

#include "../../includes.h"

int ShockMainCore::Update()
{
    //bRet = PeekMessage( &msg, NULL, 0, PM_REMOVE     ));

    //if (bRet == -1)
    //{
    //    // handle the error and possibly exit
    //}
    //else
    //{
    //    TranslateMessage(&msg); 
    //    DispatchMessage(&msg); 
    //}

    return 0;
}
